# CTF Notes 内容库

请在 Obsidian 中把本目录作为 Vault 打开。

- 正式文章放在 `posts/<slug>/index.md`。
- 图片与附件放在对应文章目录；当前 Vault 已设置为“附件与文章同目录”。
- 新文章可使用 `templates/CTF WP.md`。
- `draft: true` 的文章不会出现在生产站点。
- 文章链接和图片使用标准 Markdown 语法，不使用 `[[Wiki Link]]` 或 `![[图片]]`。

完整的 Windows/macOS 同步与发布步骤见仓库根目录 `docs/同步与发布.md`。
